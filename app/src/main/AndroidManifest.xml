<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobile.unisinos.br.tarefa3">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_local_gas_station_black_24dp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalcularMediaSimplesActivity" />
        <activity android:name=".QualCombustivelActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".CalcularMediaCompletaActivity" />
        <activity android:name=".GastoEstimadoActivity"></activity>
    </application>

</manifest>